@model List<Homework0705.Models.Supplier>

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <div>
        <a href="/Supplier/Create">Create Supplier</a>
        <table border="1" width="100%">
            <tr>
                <th>ID</th>
                <th>SupplierName</th>
            </tr>

            @if (Model.Count > 0)
            {
                @foreach (var supplier in Model)
                {
                    <tr>
                        <td>@supplier.ID</td>
                        <td>@supplier.SupplierName</td>
                    </tr>
                }
            }
            else
            {
                <tr>
                    <td colspan="2">
                        <span>Herhangi bir tedarikçi bulunamadı!</span>
                    </td>
                </tr>
            }



        </table>
    </div>
</body>
</html>
